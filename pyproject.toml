[tool.poetry]
name = "renamer"
version = "0.1.2"
description = ""
authors = ["ccdale <chris.allison@hivehome.com>"]
readme = "README.md"
packages = [{include = "renamer", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
ccalogging = "^0.4.4"

[tool.poetry.scripts]
renumber = "renamer:doRename"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
